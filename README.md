# LLM application development course

## Setup

First, install `uv`: https://docs.astral.sh/uv/getting-started/installation/

Then, run the following `uv` commands in the project root directory:

```Shell
uv sync
uv run -m ipykernel install --sys-prefix --name python3
. .venv/bin/activate
```
